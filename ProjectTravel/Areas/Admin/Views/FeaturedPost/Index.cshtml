@using ProjectTravel.Models;
@model IList<view_Featured_Post>


<div class="wrapper">
    <main id="main" class="main">
        <div class="pagetitle">
            <h2>Bài đăng nổi bật</h2>
        </div>

        <p>
            <a type="button" class="btn btn-success" asp-area="Admin" asp-controller="FeaturedPost" asp-action="Create">
                <i class="bi bi-file-earmark-text me-1"></i> Thêm menu
            </a>
        </p>

        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-12">

                        <div class="card">
                            <div class="card-header">
                                <h3 class="card-title">Thông tin Menu</h3>
                            </div>
                            <!-- /.card-header -->
                            <div class="card-body">
                                <table class="table table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Tiêu đề</th>
                                            <td>Tác giả</td>
                                            <td>Ngày đăng</td>
                                            <th>Chức năng</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <th>@item.FeaturedPostID</th>
                                                <td>@item.Title</td>
                                                <td>@item.Author</td>
                                                <td>@item.CreatedDate</td>
                                                <td>
                                                    <a asp-area="Admin" asp-controller="FeaturedPost" asp-action="Edit"
                                                       asp-route-id="@item.MenuID" class="btn btn-primary btn-sm"
                                                       title="Sửa nội dung bài viết"><i class="bi bi-pen"></i></a>

                                                    <a asp-area="Admin" asp-controller="FeaturedPost" asp-action="Delete"
                                                       asp-route-id="@item.MenuID" class="btn btn-danger btn-sm"
                                                       title="Xóa thông tin bài viết"><i class="bi bi-trash"></i></a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>

                                </table>
                            </div>
                            <!-- /.card-body -->
                        </div>
                        <!-- /.card -->
                    </div>
                    <!-- /.col -->
                </div>
                <!-- /.row -->
            </div>
            <!-- /.container-fluid -->
        </section>


    </main>
</div>
